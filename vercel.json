{
  "version": 2,
  "builds": [
    {
      "src": "client/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/client"
      }
    },
    {
      "src": "server/package.json",
      "use": "@vercel/node",
      "config": {
        "zeroConfig": false
      }
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/server/main.js" },
    { "src": "/(.*)", "dest": "/client/$1" }
  ]
}
